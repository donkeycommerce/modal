export enum ModalType {
    MESSAGE = 'message',
    TOAST   = 'toast',
}

export enum ModalScope {
    WARNING = 'warning',
    SUCCESS = 'success',
    ERROR   = 'error'
}

export default {
    ModalScope,
    ModalType
}